{
  "name": "lurepedia-e2e",
  "private": true,
  "description": "End-to-end (Playwright) tests for Lurepedia. The app itself uses no Node build step; this is dev-only test tooling.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
