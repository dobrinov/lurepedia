<%# locals: record, changeset %>
<% brand = record %>
<div class="row" style="gap:16px">
  <% if diff_changed?(changeset, :logo) %>
    <% new_blob = diff_blob(changeset["logo"][1]) %>
    <span class="avatar avatar-lg"><% if new_blob %><%= image_tag new_blob.variant(resize_to_fill: [ 120, 120 ]), alt: brand.name %><% else %><%= brand.initials %><% end %></span>
  <% else %>
    <%= render "brands/avatar", brand: brand %>
  <% end %>
  <div class="stack" style="gap:6px">
    <h1 style="font-size:28px;font-weight:700;letter-spacing:-.02em"><%= diff_field(changeset, brand, :name) %></h1>
    <div class="row muted" style="gap:8px;font-size:14px">
      <span><%= t("brand.country") %>: <%= diff_field(changeset, brand, :country) %></span>
      <span>· <%= t("brand.founded") %> <%= diff_field(changeset, brand, :founded_year) %></span>
    </div>
    <% if diff_changed?(changeset, :website) || brand.website.present? %>
      <div class="muted" style="font-size:13px"><%= t("brand.website") %>: <%= diff_field(changeset, brand, :website) %></div>
    <% end %>
    <p class="muted" style="max-width:520px"><%= diff_field(changeset, brand, :blurb) %></p>
  </div>
</div>
